# Core dependencies
click>=8.1.0
pydantic>=2.0.0
pyyaml>=6.0
jinja2>=3.1.0
requests>=2.31.0
jsonschema>=4.20.0
rich>=15.0.0
types-PyYAML>=6.0.12.20260815
types-requests>=2.33.0.20260712
argon2-cffi>=23.1.0  # Secure password hashing

# Mobile testing - Updated for Appium 3.x support
appium-python-client>=4.0.0,<7.0.0  # Appium 3.x client
selenium>=4.15.0,<5.0.0  # Required for W3C WebDriver protocol

# Testing framework
pytest>=8.0.0,<10.0.0
pytest-bdd>=6.1.0

# Static analysis (optional)
tree-sitter>=0.20.0
lxml>=4.9.0

# Mock backend (optional)
fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
flask==3.1.3  # For ML mock server

# ML & AI (Phase 4)
scikit-learn>=1.9.0
numpy>=2.5.2
pandas>=3.0.5
joblib>=1.5.3

# Image processing & OCR
pillow>=12.3.0
opencv-python>=5.0.0.93
pytesseract>=0.3.13

# Visualization & Analytics
matplotlib>=3.11.1
seaborn>=0.13.2
plotly>=6.9.0

# Development tools (optional)
# Pin black exactly: formatting changes between black releases, so an
# unpinned version makes `black --check` in CI fail against code formatted
# with a different version. Bump deliberately (and reformat) when upgrading.
black==26.5.1
flake8>=6.0.0
mypy>=1.5.0
pytest-cov>=4.1.0

# Phase 5: Enterprise Integration
pytest-xdist>=3.8.0          # Parallel test execution
pytest-rerunfailures>=16.6   # Retry failed tests
allure-pytest>=2.16.0        # Allure reporting
pytest-html>=4.2.0           # HTML reports
pytest-json-report>=1.5.0    # JSON reports
slack-sdk>=3.43.0            # Slack notifications
gitpython>=3.1.59             # Git integration
junitparser>=5.0.3           # JUnit XML manipulation

interrogate>=1.7.0            # docstring-coverage gate
